local nightfox = require('nightfox')

nightfox.setup({
    fox = 'nightfox', -- change the colorscheme
    styles = {
        comments = 'italic',
        functions = 'italic',
    },
})

-- load the configurations
nightfox.load()
